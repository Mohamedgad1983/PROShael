# Flutter Archive

Historical Flutter backup files live here for reference only. Active Flutter source belongs under `alshuail-flutter/lib/`, with platform projects under `android/`, `ios/`, `web/`, `macos/`, `linux/`, and `windows/`.
