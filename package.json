{
  "name": "proshael",
  "version": "2.0.0",
  "private": true,
  "description": "Al-Shuail Family Management System monorepo",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "backend:dev": "npm --prefix alshuail-backend run dev",
    "backend:start": "npm --prefix alshuail-backend start",
    "backend:lint": "npm --prefix alshuail-backend run lint",
    "backend:test": "npm --prefix alshuail-backend run test:unit -- --runInBand --passWithNoTests",
    "backend:security": "npm --prefix alshuail-backend run security:scan",
    "admin:dev": "npm --prefix alshuail-admin-arabic start",
    "admin:build": "npm --prefix alshuail-admin-arabic run build:fast",
    "admin:type-check": "npm --prefix alshuail-admin-arabic run type-check",
    "mobile:dev": "npm --prefix alshuail-mobile run dev",
    "mobile:build": "npm --prefix alshuail-mobile run build",
    "mobile:lint": "npm --prefix alshuail-mobile run lint",
    "check:backend": "npm run backend:lint && npm run backend:test && npm run backend:security",
    "check:admin": "npm run admin:type-check && npm run admin:build",
    "check:mobile": "npm run mobile:lint && npm run mobile:build",
    "check:audit": "npm audit --audit-level=moderate && npm --prefix alshuail-backend audit --audit-level=moderate && npm --prefix alshuail-admin-arabic audit --audit-level=moderate && npm --prefix alshuail-mobile audit --audit-level=moderate",
    "check": "npm run check:audit && npm run check:backend && npm run check:admin && npm run check:mobile",
    "docs:structure": "sed -n '1,240p' docs/PROJECT_STRUCTURE.md"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "dotenv": "^17.2.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "playwright": "^1.56.1"
  }
}
