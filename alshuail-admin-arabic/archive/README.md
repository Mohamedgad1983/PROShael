# Admin Archive

Historical admin dashboard artifacts live here for reference only. Active admin source belongs in `alshuail-admin-arabic/src/`; active tooling belongs in `alshuail-admin-arabic/scripts/` or `alshuail-admin-arabic/tools/`.

Do not commit build archives or generated bundles.
