# Backend Archive

Historical backend scripts and temporary outputs live here for reference only. Active backend scripts belong in `alshuail-backend/scripts/`, and active backend source belongs in `alshuail-backend/src/`.

Archived Supabase scripts are not part of the current PostgreSQL/VPS architecture.
